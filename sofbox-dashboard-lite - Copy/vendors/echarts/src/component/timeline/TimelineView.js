/**
 * @file Timeline view
 */
define(function (require) {

    // var zrUtil = require('zrender/core/util');
    // var graphic = require('raphic');
    var ComponentView = require('omponent');

    return ComponentView.extend({

        type: 'timeline'
    });

});